# Backend environment variables — copy to backend/.env and adjust.
# These override the root .env.example defaults when running the backend directly.

WHISPER_MODEL=base
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:5173
LOG_LEVEL=INFO
